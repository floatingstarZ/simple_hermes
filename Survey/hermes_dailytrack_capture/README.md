# Hermes DailyTrack Raw OpenAI Capture

This folder stores a reproducible capture setup for Hermes DailyTrack runs and a local viewer for studying the raw OpenAI SDK requests/responses sent by Hermes.

Files:

- `sitecustomize.py` patches the OpenAI Python SDK at process startup and records every `responses.create`, `responses.stream`, and `chat.completions.create` call.
- `run_hermes_dailytrack_capture.sh` runs `hermes chat` in `/Users/hzy/Desktop/work/DailyTrack_NewTech` with the capture hook enabled.
- `runs/<timestamp>/openai_raw_calls.json` is the aggregate raw call capture.
- `runs/<timestamp>/openai_raw_calls.jsonl` is the append-only raw call stream.
- `runs/<timestamp>/hermes_stdout.txt` is Hermes stdout/stderr.
- `runs/<timestamp>/track_results/` contains copied DailyTrack deliverables from the run.
- `runs/<timestamp>/trace_summary.json` is a compact call index generated by `summarize_trace.py`.
- `visualize_traces.py` serves a local browser UI for all captured runs and raw call traces.
- `run_trace_viewer.sh` starts the visualization service.
- `summarize_trace.py` builds a compact summary for a captured run.
- `Analysis.md` summarizes the captured request/response structure and prompt/tool findings.

The capture records SDK-level request kwargs and raw SDK response objects. It does not record Authorization headers, but it does contain user prompts, tool arguments, tool outputs, local paths, repository content, and command text. Treat the trace files as sensitive research artifacts.

## Captured Run

The main completed run is:

```text
runs/20260417_164151
```

Run facts:

- Exit code: `0`
- Canonical LLM turns: `45` `responses.stream` calls
- Duplicate SDK wrapper records: `45` `responses.create` calls
- Total captured records: `90`
- Model: `gpt-5.4`
- Raw JSONL size: about `97 MB`
- Aggregate JSON size: about `108 MB`
- Main DailyTrack results copied to `runs/20260417_164151/track_results/dailytrack_2026-04-16/`
- Raw collection files copied to `runs/20260417_164151/track_results/raw_tmp_dailytrack_2026_04_16/`

For analysis, prefer `responses.stream` records. In this capture, every actual Hermes turn appears as one `responses.stream` record plus one associated `responses.create` wrapper record. The `responses.stream` record has the complete raw response object and stream events; the paired `responses.create` record only stores a synthetic note such as `streaming response captured as stream_events`.

Run:

```bash
cd /Users/hzy/Desktop/work/simple_hermes/Survey/hermes_dailytrack_capture
./run_hermes_dailytrack_capture.sh
```

View traces:

```bash
cd /Users/hzy/Desktop/work/simple_hermes/Survey/hermes_dailytrack_capture
./run_trace_viewer.sh
```

The viewer prints the actual localhost URL. It starts at port `8765` and automatically tries later ports if the preferred port is busy.

The viewer scans `runs/*/openai_raw_calls.jsonl` plus top-level trace directories such as smoke runs, selects the newest run by default, and opens the newest captured call in that run. Use the run selector and trace list to inspect raw request kwargs, raw responses, stream events, and full JSON records.

## Viewer UI Design

The viewer is designed around two trace-reading modes:

- Raw inspection: `Request`, `Response`, `Stream Events`, and `Full Raw` preserve the SDK-level objects for exact debugging.
- Append-only inspection: `Append View` shows how Hermes reconstructs each model request from the previous turn.

`Append View` uses three columns:

| Column | Meaning |
|---|---|
| Incoming Append | For the first canonical turn, this shows the static prompt envelope: request params, `instructions`, `tools`, and initial user input. For later turns, it shows `current.input[len(previous.input):]`. |
| Response Items | Shows only model-output items that are relevant to the next request append. It excludes full response metadata and keeps tool outputs out of this column. |
| Outgoing Append | Shows `next.input[len(current.input):]`, including model-output items plus `function_call_output` items. The final canonical turn has no next request, so outgoing append is not observable. |

The append columns intentionally have no default character truncation. Long tool outputs remain scrollable in-place so the raw append boundary is visible. General request/response preview tabs still use truncation to keep the page responsive.

Visual hierarchy:

- Left sidebar is a run/call navigator with compact metadata.
- Active calls use a green accent and hover shadow.
- Append View uses distinct header colors: incoming context, model response items, and outgoing append.
- Panels use stable dimensions and scrollable code blocks to avoid layout shifts while switching calls.

Summarize a run:

```bash
cd /Users/hzy/Desktop/work/simple_hermes/Survey/hermes_dailytrack_capture
./summarize_trace.py 20260417_164151
```

Read the analysis:

```bash
open /Users/hzy/Desktop/work/simple_hermes/Survey/hermes_dailytrack_capture/Analysis.md
```
